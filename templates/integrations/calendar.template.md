# Google Calendar — My Setup

**Role:** calendar
**Verified:** never — run the `setup` skill to populate and verify this file
**Health check:** <cheapest read-only tool call that proves this connector is live, e.g. a list call>

Personal specifics for Google Calendar (calendar IDs, conventions). Generic logic lives in the `calendar` skill. Fill in and delete guidance lines. Skip if unused.

## Access
- Available via calendar MCP tools (account connector or self-registered — see README § Connectors; then run the `setup` skill).
- Timezone: <IANA tz> — always use this.

## My calendars
| Calendar | ID | Use for |
|----------|-----|---------|
| <primary> | <id/email> | <default> |
| ... | | |

Read-only/reference calendars: <holidays etc.>

## Conventions
- <title style, metadata usage, which calendar for which kind of event>
