# ClickUp — My Setup

**Role:** task-system
**Verified:** never — run the `setup` skill to populate and verify this file
**Health check:** <cheapest read-only tool call that proves this connector is live, e.g. a list call>

Personal specifics for ClickUp (IDs, structure, workflow). Generic operating logic lives in the `clickup` skill. Fill in and delete guidance lines. Skip this file entirely if you don't use ClickUp.

## Access
- Available via `clickup_*` MCP tools (account connector or self-registered — see README § Connectors; then run the `setup` skill).
- Workspace ID: <id>
- You: <name> (user id <id>, <email>)

## How you use ClickUp
- <your philosophy — minimal? full-featured? what layers of tasks/docs?>

## Key resources
- Main hub doc/list: <doc id>, <page id>, <url>
- Default list for new tasks: <list id>
- Other docs/lists: <table of ids>

## Workflow
- <daily loop>
- <weekly loop / sync to other tools>

## Conventions & safety
- <naming, metadata usage>
- Note any API gotchas (e.g. doc-page updates overwrite the whole page → snapshot to tmp/ first).
